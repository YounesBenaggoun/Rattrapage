info:
  name: localhost:5000/user/login VISITOR
  type: http
  seq: 1

http:
  method: POST
  url: localhost:5000/user/login
  body:
    type: json
    data: |-
      {
          "email" : "visitor@gmail.com",
          "password" : "12341234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
