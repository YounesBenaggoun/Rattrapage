info:
  name: localhost:5000/user/login EXPOSER
  type: http
  seq: 3

http:
  method: POST
  url: localhost:5000/user/login
  body:
    type: json
    data: |-
      {
          "email" : "exposer@gmail.com",
          "password" : "12341234"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
